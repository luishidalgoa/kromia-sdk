{
  "name": "@kromia/core",
  "version": "2.4.0",
  "private": true,
  "description": "Core del SDK Kromia. Source-of-truth del modelo (recipes, behaviors, actions, slot kinds, field types) + helpers (classifyField, validateComposition, formatScalar, synthSectionItems). Emite contracts/*.json al regenerar. Consumido por Studio (Next.js), Backend (NodeJS), y futuro Flutter via @kromia/flutter.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "gen": "tsx src/generate.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
