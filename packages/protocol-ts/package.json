{
  "name": "@kromia/protocol",
  "version": "1.8.0",
  "private": true,
  "description": "SDK TypeScript del contrato Kromia. Source-of-truth del modelo (recipes, behaviors, actions, slot kinds, field types) + helpers (classifyField, isFieldCompatibleWithSlot). Emite contracts/*.json al regenerar.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "gen": "tsx src/generate.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
