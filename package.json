{
  "name": "kromia-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo del contrato Kromia: contracts/ (JSON + schemas) + playbooks/ (procesos) + packages/protocol-ts (SDK TypeScript). Futuro packages/protocol-dart cuando Flutter exista.",
  "type": "module",
  "scripts": {
    "gen": "pnpm --filter @kromia/protocol gen",
    "test": "pnpm --filter @kromia/protocol test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.0.0"
}
